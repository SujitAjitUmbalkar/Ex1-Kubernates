apiVersion: v1
kind: Pod
metadata:
  name: my-ngnix-pod
  labels:
    app.kubernetes.io/name: myapp
spec:
  containers:
  - name: my-ngnix-container
    image: nginx
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
